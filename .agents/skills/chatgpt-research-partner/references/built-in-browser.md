# Codex built-in browser runbook

- Confirm Browser is available in the ChatGPT desktop app and the task is running
  in Codex/Work, not CLI-only or IDE-only surfaces.
- Keep the browser task visible during initial authentication and critical model
  selection.
- Use the saved Project URL rather than sidebar position.
- Verify active page title, Project name, and model badge after each critical
  navigation.
- If the active Windows desktop is unavailable, Computer Use may stop; preserve
  local consultation state and resume later.
