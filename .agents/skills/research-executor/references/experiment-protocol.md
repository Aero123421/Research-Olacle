# Executor experiment protocol

For every branch, register the observation, falsifiable hypothesis, decision it
can change, cheapest discriminating test, expected outcomes/actions, exact
configuration, commit, runtime, resources, artifacts, and result. Use
Smoke → Quick → Full → Confirm. Preserve valid negative results and reject runs
with leakage, metric errors, row-order mismatch, or unreproducible state.
