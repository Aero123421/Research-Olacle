# Compute policy

Not initialized. Bootstrap must set local GPU availability, wall/GPU limits,
paid-compute hard limits, typed resources, approved backends, provider metering, and enforced cancellation. Paid compute remains disabled until a reviewed code-registered control adapter proves both provider-side cost metering and stop confirmation.
