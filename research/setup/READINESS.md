# Research Lab Readiness

Not initialized. Run on the Windows research host:

```powershell
researchctl doctor --profile full
```

Doctor writes capability-specific results here. Optional advisor failures do not
block the core Planner–Executor loop; missing core state, Codex Goal execution,
or all compute paths do.
