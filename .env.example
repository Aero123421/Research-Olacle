# This project intentionally does not require API keys in repository files.
# Keep credentials in the provider's official login store or a secure local
# secret manager. Never copy real values into this file.
